/**
 * @swagger
 * components:
 *   schemas:
 *     UpdateUser:
 *       type: object
 *       properties:
 *         firstName:
 *           type: string
 *         lastName:
 *           type: string
 *         userName:
 *           type: string
 *         userType:
 *           type: string
 *         file:
 *          type: string
 *          format: binary
 *         email:
 *           type: string
 *         phoneNumber:
 *           type: string
 *         password:
 *           type: string
 *         verified:
 *           type: boolean
 *         roles:
 *            type: string[]
 */
